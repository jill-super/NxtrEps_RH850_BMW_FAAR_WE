---
title: 'TL109A SwcSuprt'
description: 'TL109A_SwcSuprt (TOOLS). Host-side tool / generator package: TL109A_SwcSuprt.'
---

:::tip[Origin: Custom · Nexteer in-house]
:::

## Purpose

Host-side tool / generator package: TL109A_SwcSuprt.

*Repository path:* `TL109A_SwcSuprt/` · *AUTOSAR layer:* [Tools & Support](../) · *Implementation tag:* `Impl (implementation package)`

## Key files

| Area | Files |
| --- | --- |
| `src/` (0 C file(s)) | — |
| `include/` (0 header(s)) | — |
| `autosar/` (0 ARXML) | — |
| Build/config | `tools/` |
| Review artefacts | 1 spreadsheet checklist(s) (`*.xls*`, not converted) |

## Public API (excerpt)

No `FUNC(...)` AUTOSAR-style entry points were detected in the scanned headers (the module may expose RTE ports, generated interfaces, or data tables instead). See the key files above and the design documents below.

## Dependencies (excerpt)

No quoted `#include "..."` dependencies were found in the scanned sources.

## Documents

Converted from the module `doc/` folder (Word/PDF → Markdown excerpts):

- [SwcSuprt_Usage](../tl109a_swcsuprt__swcsuprt-usage-docx/)

Source files remain in the repository next to this documentation.

