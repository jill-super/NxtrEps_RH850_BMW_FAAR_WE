---
title: 'TL119A Python3'
description: 'TL119A_Python3 (TOOLS). Host-side tool / generator package: TL119A_Python3.'
---

:::tip[Origin: Custom · Nexteer in-house]
:::

## Purpose

Host-side tool / generator package: TL119A_Python3.

*Repository path:* `TL119A_Python3/` · *AUTOSAR layer:* [Tools & Support](../) · *Implementation tag:* `Impl (implementation package)`

## Key files

| Area | Files |
| --- | --- |
| `src/` (0 C file(s)) | — |
| `include/` (0 header(s)) | — |
| `autosar/` (0 ARXML) | — |
| Build/config | `tools/` |
| Review artefacts | 1 spreadsheet checklist(s) (`*.xls*`, not converted) |

## Public API (excerpt)

No `FUNC(...)` AUTOSAR-style entry points were detected in the scanned headers (the module may expose RTE ports, generated interfaces, or data tables instead). See the key files above and the design documents below.

## Dependencies (excerpt)

No quoted `#include "..."` dependencies were found in the scanned sources.

## Documents

No Word/PDF documents were found for this module.

